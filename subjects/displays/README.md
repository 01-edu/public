## displays

### Instructions

Write a program that displays an `S` character on the standard folowed by newlines (`'\n'`)

### Usage

```console
$ go run . | cat -e
S$
$ go run . "a" "b" | cat -e
S$
$ go run . "a" "b" "c" | cat -e
S$
```
